<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <title></title>
  </head>
  <body>
    <script>
        let word0="Wassup";
        let word0Translation=("здарова");
        let word1="Bye";
        let word1Translation=("Покеда");
        let word2="Programmer";
        let word2Translation=("Программист");

        let finishMessage="Молодец";

    
        let userAnswer0=prompt(word0);
        allert(userAnswer0===word0Translation);
        let userAnswer1=prompt(word1);
        allert(userAnswer1===word1Translation);
        let userAnswer2=prompt(word2);
        allert(userAnswer2===word1Translation);
    
    
        allert(finishMessage);
    </script>
</body>
</html>
